groups:
- name: inference-alerts
  rules:

  - alert: HighErrorRate
    expr: rate(error_total[5m]) / rate(requests_total[5m]) > 0.05
    for: 1m

  - alert: HighP99Latency
    expr: histogram_quantile(0.99, rate(request_latency_seconds_bucket[5m])) > 0.5
    for: 3m